/* launch.json */
{
    "version": "2.0.0",
    "configurations": [

        {
            "name": "lldb123",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/Main",
            "args": [
                ]
        }
    ]
}